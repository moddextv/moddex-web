# prettier reads .gitignore as well, so .next/ and node_modules/ need no repeat
# here. This file is only for what git tracks but prettier must not rewrite.

# npm owns this file and rewrites it on every install
package-lock.json
